<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sda.oscail.edu.actiontabs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
   <!--
       <fragment
            android:name=".ProdctsFragment"
            android:label="Stores List"
            android:parentActivityName=".MainActivity">
        </fragment>
       -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <!-- https://developer.android.com/guide/components/intents-common#CameraStill -->
                <action android:name="android.intent.action.STILL_IMAGE_CAMERA" />
            </intent-filter>

        </activity>
        <!--
        <fragment
            android:name=".OrderFragment"
            android:parentActivityName=".MainActivity"
            android:label = "Order T-Shirt">
        </fragment>
        -->
    </application>

</manifest>